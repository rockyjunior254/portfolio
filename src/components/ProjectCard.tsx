import Link from 'next/link'; import { ProjectGallery } from './ProjectGallery'; import type { Project } from '@/lib/projects';
export function ProjectCard({project,index}:{project:Project;index:number}){const type=index===0?'lms':index===1?'ttms':'pos';return <article className="project-tile"><div className="tile-meta"><span>0{index+1}</span><span>{project.eyebrow}</span></div><ProjectGallery project={type}/><div className="tile-bottom"><div><h3>{project.name}</h3><p>{project.description}</p></div><Link href={`/work#${type}`} aria-label={`Explore ${project.name}`}>↗</Link></div></article>}
